// Will the code below raise an error?
let numbers = [1, 2, 3];
numbers[6] = 5;

// Solution: No it won't. It will add '5' to the 6th index of the array. Indexes 4 and 5 will be left blank.
// Bonus: numbers[4] will return undefined.