/**
 * What do you think the following code will output?
 * 
 */
let nanArray = [NaN];

console.log(nanArray[0] === NaN); // false

/**
 * In order to reliably test if a value is NaN we can use the Number.isNaN() method.
 * 
 */

