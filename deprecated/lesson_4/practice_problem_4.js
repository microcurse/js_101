['ant', 'bear', 'caterpillar'].pop().length;

// The length of this call is 11 because pop is destructive and removes an element from the original array. The expression then calls length on the newly removed element (last element) of the array.